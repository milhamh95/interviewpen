# O(n) linear time
- in array, linked list -> find an element

## Multi Lineaar
- merge (l1, l2)
- `O(l1 + l2)`
- or `O(max(l1 + l2))`

## Caching
- using cache
  - we can reduce time complexity
  - but we increase space complexity